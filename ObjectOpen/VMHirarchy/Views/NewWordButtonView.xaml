<UserControl x:Class="VMHirarchy.Views.NewWordButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VMHirarchy.Views"
             xmlns:vm="clr-namespace:VMHirarchy.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="180"
             d:DataContext="{d:DesignInstance IsDesignTimeCreatable=False, Type={x:Type vm:NewWordButtonVM}}">

    <UserControl.DataContext>
        <StaticResource ResourceKey="ButtonVM"/>
    </UserControl.DataContext>

    <Grid>
        <Button Content="Get next plugin name"
                Command="{Binding FetchNewWord}"/>
    </Grid>
</UserControl>
